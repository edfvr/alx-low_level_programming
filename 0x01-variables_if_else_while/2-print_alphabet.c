#include <stdio.h>
#include <stlib.h>
#include <time.h>

/**
 * *main - prints the alphabet in lowercase, followed by a new line.
 * *Return: 0 to exit successfully
 * */

int main(void)
{
       	int i;
       	for(i = 97; i <= 122; i++)
       	{
		putchar(i);
	}
	putchar(10)
	return (0);
}
